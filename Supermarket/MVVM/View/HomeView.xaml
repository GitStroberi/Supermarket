<UserControl x:Class="Supermarket.MVVM.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Supermarket.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="AliceBlue"
             >
    <Grid>
        <TextBlock Text="Logged User: " HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0"/>
        <TextBlock Text="{Binding LoggedIn}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="100,10,0,0"/>

        <Button Content="Check" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,40,0,0" Command="{Binding CheckPrivilegeCommand}"/>
    </Grid>
</UserControl>
